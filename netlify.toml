[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so deep links serve index.html instead of 404 (prevents HTML being returned for JS asset requests)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
