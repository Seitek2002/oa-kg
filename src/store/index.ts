import ocrReducer, {
  setPassportData,
  clearPassportData,
  setIdentificationImages,
} from './ocrSlice';
import transactionReducer, {
  setTransaction,
  clearTransaction,
} from './transactionSlice';

export {
  ocrReducer,
  setPassportData,
  clearPassportData,
  setIdentificationImages,
  transactionReducer,
  setTransaction,
  clearTransaction,
};
